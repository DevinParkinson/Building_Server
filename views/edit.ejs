<html>
  <head>
    <% include ./partials/head %>
  </head>
  <body>
    <header>
      <% include ./partials/header %>
    </header>
    <div class="container">
      <% var url = "/soccer/" + teams.id + "?_method=PUT" %>
      <form method="POST" action=<%= url %> >
        <input required name="title" value=<%= teams.title %> />
        <a href="/soccer" class="btn red">Cancel</a>
        <button class="btn">Save</button>
     </form>
   </div>
  </body>
</html>
